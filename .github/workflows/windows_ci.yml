name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: mkdir -p build && echo "Hello" > build/artifact.txt
      - uses: actions/upload-artifact@v4
        with:
          name: my-artifact
          path: build/artifact.txt
